{% extends 'base.html.twig' %}

{% block title %}New Mail{% endblock %}

{% block body %}
     <div class="container">
        <h2 class="text-center mt-5 mb-3">Create New Mail</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-primary" href="{{ path('index') }}"> 
                    Home 
                </a>
            </div>
            <div class="card-body">
            {{ include('mail/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
